{
  "name": "DatabaseConnection",
  "description": "Stores database connection information for multiple database types",
  "properties": {
    "name": {
      "type": "string",
      "description": "Friendly name for the connection"
    },
    "database_type": {
      "type": "string",
      "description": "Type of database (postgresql, mysql, sqlite, mongodb, etc.)"
    },
    "host": {
      "type": "string",
      "description": "Database host address (not required for SQLite)"
    },
    "port": {
      "type": "number",
      "description": "Database port number"
    },
    "database": {
      "type": "string",
      "description": "Database name or file path for SQLite"
    },
    "username": {
      "type": "string",
      "description": "Database username (not required for SQLite)"
    },
    "password": {
      "type": "string",
      "description": "Encrypted database password"
    },
    "ssl_mode": {
      "type": "string",
      "description": "SSL connection mode"
    },
    "connection_string": {
      "type": "string",
      "description": "Full connection string for databases that support it"
    },
    "is_active": {
      "type": "boolean",
      "description": "Whether this connection is currently active"
    },
    "last_connected": {
      "type": "string",
      "description": "Last successful connection timestamp"
    }
  }
}